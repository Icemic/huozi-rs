# 活字 — Agent 指南

本文档面向 AI coding agent 与开发者。

---

## 项目速览

（待编写）

- `docs/architecture.md`：记录架构设计、模块边界、数据模型、API 设计和行为规范。
- `docs/iteration/*.md`：历史迭代记录，文件名遵循 `docs/iteration/<YYYY-MM-DD>-<category>-<name>.md`。

---

## 通用编码规范

- **最小化改动**：实现新功能或修 bug 时，只改必要部分，不顺手重构无关代码。
- **从根本上解决问题**：拒绝补丁式修复、不叠床架屋；分析根本原因后再设计解决方案。
- **不确定就提问**：需要较大重构、需求不明、有歧义时，停下来用提问工具确认，不要自行猜方案。
- **拒绝矛盾需求**：需求互相冲突时，拒绝修改并给出详细解释，由用户决定取舍。
- **先读再写**：修改前先阅读相关代码，避免重复内容或与既有设计冲突。
- **性能意识**：尽量使用 O(1)、O(n) 等低复杂度的算法，不要出现 O(n^2) 或更高复杂度的操作，避免不必要的性能开销。同时兼顾内存占用和分配次数，避免不必要的 heap 分配。
- **注释一律中文**：本项目采用母语优先原则。代码文件中的注释必须中文书写。旧的英文注释在涉及变更时应逐步改为中文，但与本次改动无关的英文注释不必修改。
- **对话用用户偏好语言**：与用户的自然语言交流默认用中文。
- **当前处于开发阶段**：不需考虑向后兼容或用户数据迁移，但仍要关注可维护性与可扩展性。

### 任务执行流程

1. 接到多件事情时，先整体 review + 调研；有疑问先用提问工具询问，再开始实施。
2. 按顺序逐项实施（除非互相关联），可自行安排顺序。
3. 实施过程中如发现设计或计划有问题、有更好方案，提出讨论后再继续。
4. 每次实施完成后：
   - 检查编辑器报错并修复；
   - 自行判断是否需要更新 `docs/architecture.md`、`docs/iteration/*.md`，按需更新；迭代文档只应该更新当前迭代对应的文件，不要编辑历史记录。
   - 检查相关测试是否需要更新；
   - 再次 review，对照本规范与设计文档，检查是否有遗漏或违规。
5. 修改既有功能行为前，先确认改动范围与影响，必要时询问用户决策。

## 文档与规范驱动开发（DDD）

本仓库采用文档与规范驱动开发。稳定结论分别维护在：

- `docs/architecture.md`：记录架构设计、模块边界、数据模型、API 设计和行为规范。
- `docs/iteration/<YYYY-MM-DD>-<category>-<name>.md`：历史迭代记录，某项新需求或较大修复的迭代记录。

迭代文档的 `category` 只能使用以下值：

| 分类 | 使用边界 |
| --- | --- |
| `feat` | 新增或改变公开能力、数据模型、API、布局消费能力或架构能力。 |
| `perf` | 主要目标是减少运行时间、内存、分配或重复计算，且既有行为保持不变。 |
| `bugfix` | 修复既有实现与预期行为不一致的问题，并应有对应回归验证。 |
| `refactor` | 只调整内部结构、模块边界、所有权或调用路径，不以新增能力或性能收益为主要目标。 |
| `test` | 主要建设或迁移测试、fixture、golden、测试 runner 或验证基础设施。 |

分类按迭代的主要目标确定，不按实施过程中出现的代码手段确定。判断顺序为：公开能力或数据模型改变使用 `feat`；主要验收指标是时间、内存或分配减少使用 `perf`；修复错误行为使用 `bugfix`；只改变内部实现结构使用 `refactor`；主要建设测试和验证资产使用 `test`。

一份迭代文档只使用一个主分类，不使用复合分类名。`refactor` 不表示“包含重构”，只有在没有新增能力、性能目标或错误修复目标时才使用。其他文档目录继续遵循各自的命名规则，不套用本规则。

### 工作流程

1. 接到新需求或多项任务时，先阅读持续维护文档、相关代码和现有测试。
2. 对新增特性或算法调整，先创建迭代文档，写清目标、范围、设计、兼容性、验证和回滚方案。
3. 按迭代文档实施；若实现中需要改变设计，先讨论确认，再更新迭代文档。
4. 完成后运行相关验证，并对照迭代文档或相关设计文档，检查是否符合预期。

小型、无行为变化的维护（例如明确的拼写修正）不应新建迭代文档，但仍需保持持续维护文档与实际状态一致。

## 取舍

1. 静默失败优先：作为游戏引擎组件，应尽量避免终止流程，而是尽量在局部退化后继续处理，可以输出固定文本 `warn`，但不应为日志额外扫描、排序、clone、收集或格式化复杂上下文。

---

## 测试、开发与发布

见 [Readme](README.md)。
